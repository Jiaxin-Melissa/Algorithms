class Solution:
    def sortArrayByParity(self, A: List[int]) -> List[int]:
        evens=[A[i] for i in range(0,len(A)) if A[i]%2==0]
        odds=[A[i] for i in range(0,len(A)) if A[i]%2!=0]
        return (evens+odds)
